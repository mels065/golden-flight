 <div class="container d-flex justify-content-center">
   <h4><strong>{{departingCity}}</strong> to <strong>{{arrivingCity}}</strong></h2>
   </div>

<div class="container px-4 flight-results">
  <div class="row results">
    <div class=" col-sm-6 col-md-3">
      {{Airliner.name}}
    </div>
    <div class="col-sm-6 col-md-3">
      {{takeOffTime}} - {{landingTime}}
    </div>
    <div class="col-sm-6 col-md-3">
      {{price}}
    </div>
    <a href="/flight/{{id}}" class="col-sm-6 col-md-2 book-flight">
      Book flight
    </a>
  </div>
 </div>
